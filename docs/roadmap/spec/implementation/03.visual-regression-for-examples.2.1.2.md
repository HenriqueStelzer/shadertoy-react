---
id: visual-regression-for-examples
version: 2.1.2
phase: P1
status: done
priority: 03
roadmap_anchor: "#visual-regression-for-examples"
depends_on: []
---

# Visual regression for examples

## Goal

Catch GPU/render regressions in the demo grid.

## Current state

Done on `test/sandbox-playground`. `npm run test:visual`: 16 passed, camera skipped (CI + local).

## Deliverables

- [x] `playwright.config.ts` — serves `examples/dist` on `:3001`, viewport **1280×720**
- [x] `tests/visual/demo-grid.spec.ts` — `?solo=demo-*` per tile, scroll cue, backing-store check
- [x] `tests/visual/demo-links.ts` tolerance map
- [x] `.cursor/skills/visual-regression/` skill and `demo-visual-qa` rule
- [x] `.github/workflows/ci.yml` visual test job
- [x] Solo-tile layout (`html[data-solo-tile]`) for stable ≈427×240 snapshots

## Acceptance criteria

1. `npm run test:visual` passes locally
2. CI runs Playwright on PR to `main` (after branch merge)
3. All 15 demo tiles have snapshot baselines

## Files and commands

| Path / command | Purpose |
|----------------|---------|
| `playwright.config.ts` | Test server config |
| `tests/visual/` | Specs and tolerances |
| `examples/src/index.tsx` | `?solo=` tile filter |
| `npm run test:visual` | Run visual regression |
| `npm run build` | Build examples/dist first |

## Out of scope

- Unit tests for shader preprocessor (spec 14)

## Handoff

Paired review spec: [`../review/03.visual-regression-for-examples.2.1.2.md`](../review/03.visual-regression-for-examples.2.1.2.md)
