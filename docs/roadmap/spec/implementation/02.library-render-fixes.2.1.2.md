---
id: library-render-fixes
version: 2.1.2
phase: P1
status: done
priority: 02
roadmap_anchor: "#library-render-fixes-212"
depends_on: []
---

# Library render fixes (2.1.2)

## Goal

Fix canvas sizing, mouse coords, texture orientation, and multi-pass compile errors discovered during visual QA.

## Current state

Done on `test/sandbox-playground`. StackBlitz resolves npm `^2.1.0` until `2.1.2` publish.

## Deliverables

- [x] `syncCanvasSize()` + `ResizeObserver` in `src/index.jsx`
- [x] `toShaderPixelCoords()` mouse mapping
- [x] Defer shader compile when `textures.length > 0` until load completes
- [x] `toGlFlipY()` in `src/Texture.js`
- [x] Multi-pass inject `sampler2D iChannelN` for pass `inputs`
- [x] `docs/textures.md` — `flipY` behavior documented

## Acceptance criteria

1. Demo-sandbox `/Textures` renders right-side up on npm after publish
2. Mouse demos correct at CSS-scaled canvas sizes
3. Multi-pass final pass compiles without undeclared `iChannelN`
4. `npm run transpile` produces updated `lib/`

## Files and commands

| Path / command | Purpose |
|----------------|---------|
| `src/index.jsx` | ResizeObserver, syncCanvasSize, toShaderPixelCoords, compile deferral |
| `src/Texture.js` | toGlFlipY |
| `docs/textures.md` | flipY docs |
| `npm run transpile` | Rebuild lib bundle |

## Out of scope

- Example grid scroll UX (2.1.0)
- Playwright baselines (separate spec 03)

## Handoff

Paired review spec: [`../review/02.library-render-fixes.2.1.2.md`](../review/02.library-render-fixes.2.1.2.md)
