---
id: ci-on-pull-requests
version: 2.1.4
phase: P1
status: partial
priority: 06
roadmap_anchor: "#ci-on-pull-requests"
depends_on: ["visual-regression-for-examples"]
---

# CI on pull requests

## Goal

Catch broken builds before merge.

## Current state

`.github/workflows/ci.yml` on `test/sandbox-playground`. Visual tests green. Active on `main` after merge. Changelog-dirty check and Dependabot (spec 13) still open.

## Deliverables

- [x] `.github/workflows/ci.yml` on branch
- [x] CI: `npm ci`, transpile, build, `test:visual`
- [x] Playwright Chromium install in CI
- [x] Visual tests stable (solo-tile + 1280×720 baselines)
- [ ] Optional: changelog-dirty check on version bumps

## Acceptance criteria

1. PR to `main` triggers green CI
2. Failed transpile/build blocks merge

## Files and commands

| Path / command | Purpose |
|----------------|---------|
| `.github/workflows/ci.yml` | Workflow definition |
| `npm run test:visual` | Visual test script |

## Out of scope

- Dependabot (spec 13)
- Unit tests (spec 14)

## Handoff

Paired review spec: [`../review/06.ci-on-pull-requests.2.1.4.md`](../review/06.ci-on-pull-requests.2.1.4.md)
