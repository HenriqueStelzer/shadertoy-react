---
id: delete-stale-remote-branches
version: 2.0.1
phase: P0
status: partial
priority: 01
roadmap_anchor: "#delete-stale-remote-branches"
depends_on: []
---

# Delete stale remote branches

## Goal

Clean GitHub branch list after merge.

## Current state

`release/2.0.0` removed. `modernize-react-webgl2` and merged doc branches may still exist on remote.

## Deliverables

- [ ] Delete `modernize-react-webgl2` (merged via PR #1)
- [ ] Delete `chore/changelog-2.0.1-alignment`, `release/2.0.1` after doc PRs merge
- [ ] Delete stale `feature/2.1.x` after hygiene pass

## Acceptance criteria

1. No merged-only branches remain on `origin`
2. `git branch -r` shows only active release/feature branches

## Files and commands

| Path / command | Purpose |
|----------------|---------|
| `git branch -r` | List remote branches |
| GitHub UI / `gh api` | Delete branches |

## Out of scope

- Local branch cleanup only without remote verification

## Handoff

Paired review spec: [`../review/01.delete-stale-remote-branches.2.0.1.md`](../review/01.delete-stale-remote-branches.2.0.1.md)
